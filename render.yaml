services:
- type: web
    name: nam-findz-frontend
    runtime: static
    buildCommand: npx pnpm install && npx pnpm --filter @workspace/namfindz run build
    staticPublishPath: artifacts/namfindz/dist
    envVars:
      - key: VITE_API_URL
        value: https://nam-findz-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

 - type: web
    name: nam-findz-frontend
    runtime: static
    buildCommand: npx pnpm install && npx pnpm --filter @workspace/namfindz run build
    staticPublishPath: artifacts/namfindz/dist
    envVars:
      - key: VITE_API_URL
        value: https://nam-findz-api.onrender.com